version: 3
repos:
  - id: /.*/
    allowed_overrides:
      - delete_source_branch_on_merge
projects:
  - name: EIC-Internal-Atlatis-Demo
    dir: .
    terraform_version: v1.3.2
    autoplan:
      when_modified:
        - "*.tf"
        - "*.tfvars"
        - "*/*.tfvars"
      enabled: true
