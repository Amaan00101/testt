version: 3

automerge: true
delete_source_branch_on_merge: true
parallel_plan: true
parallel_apply: false

repos:
  - id: /.*/

projects:
  - name: my-infra
    dir: .
    autoplan:
      when_modified:
        - "*.tf"
        - "*.tfvars"
      enabled: true
